services:
  - type: web
    name: weather-rain-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn rain_prediction_ml:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
```
4. Click **"Commit changes"**

### **Important: Check Your Files**
Your repository should now have:
- ✅ `rain_prediction_ml.py`
- ✅ `requirements.txt`
- ✅ `render.yaml`
- ✅ `README.md` (auto-created)

---

## **Step 4: Create Render Account** (1 minute)

### **Go to Render:**
https://render.com

### **Sign Up:**
1. Click **"Get Started"**
2. Click **"Sign in with GitHub"** (easier than email!)
3. Authorize Render to access your GitHub

✅ **Done!** You're logged into Render.

---

## **Step 5: Deploy Your API** (2 minutes)

### **In Render Dashboard:**

1. Click **"New +"** (top right)
2. Select **"Web Service"**

### **Connect Repository:**
1. Find your `weather-rain-api` repository
2. Click **"Connect"**

### **Configure Settings:**
Render should auto-detect everything from `render.yaml`, but verify:

- **Name:** `weather-rain-api` ✅
- **Environment:** `Python 3` ✅
- **Build Command:** `pip install -r requirements.txt` ✅
- **Start Command:** `gunicorn rain_prediction_ml:app` ✅
- **Plan:** **Free** ✅ (select this!)

### **Deploy:**
1. Scroll down
2. Click **"Create Web Service"**

---

## **Step 6: Wait for Deployment** (3-5 minutes)

You'll see a build log showing:
```
==> Downloading buildpacks...
==> Installing Python...
==> Installing dependencies from requirements.txt...
Flask==3.0.0 ✓
requests==2.31.0 ✓
gunicorn==21.2.0 ✓
==> Build successful!
==> Starting service...
Your service is live! 🎉
```

✅ **Done!** Your API is now online!

---

## **Step 7: Get Your Public URL** 

At the top of your Render dashboard, you'll see:
```
https://weather-rain-api-xyz.onrender.com
```

This is your **permanent URL**! Copy it.

---

## **Step 8: Test Your API** 

### **Open in Browser:**
```
https://weather-rain-api-xyz.onrender.com/rain-forecast?city=Chennai
```

### **Try Different Endpoints:**
```
https://your-url.onrender.com/rain-forecast?city=London
https://your-url.onrender.com/rain-forecast?city=Mumbai
https://your-url.onrender.com/predict-rain?city=Tokyo
https://your-url.onrender.com/model-info
```

---

## **Step 9: Share With Anyone!** 🎉

**Your API is now live!** Anyone in the world can access it:
```
https://weather-rain-api-xyz.onrender.com/rain-forecast?city=Chennai
